import { initProfile } from "@/lib/authLib";
import { redirect } from "next/navigation";

const Setup = async () => {

  return (
    <div>
      <h1>Will configure user</h1>
    </div>
  );
};

export default Setup;
